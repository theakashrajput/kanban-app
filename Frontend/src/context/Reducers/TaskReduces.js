export const initialState = {
    tasks: []
};

export const TaskReducer = (state, action) => {
    switch (action.type) {
        case "UPDATE_STATUS":

        case "ADD_NEW_TASK":

        case "CLEAR_ALL_COMPLETED_TASKS":
    }
}